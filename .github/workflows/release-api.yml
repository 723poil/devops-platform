name: Release Tag
on:
  pull_request:
    branches:
      - deploy/api
    types:
      - Closed
    
permissions:
  contents: write

jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v2
    - name: 버전 정보 추출
      run: |
        echo "${{ github.event.head_commit.message }}"
        echo "version=$(echo 'api-v${{ github.event.head_commit.message }}' | egrep -o '[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}')"
        echo "version=$(echo 'api-v${{ github.event.head_commit.message }}' | egrep -o '[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}')" >> "$GITHUB_OUTPUT"
      id: extract_version_name
    
    - name: Release 생성 
      uses: actions/create-release@v1
      env: 
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with: 
        tag_name: ${{ steps.extract_version_name.outputs.version }}
        release_name: ${{ steps.extract_version_name.outputs.version }}
